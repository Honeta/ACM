#include <iostream>
int main() {
    int t;
    std::cin >> t;
    while (t--) {
        int n, R;
        std::cin >> n >> R;
        for (int i = 1; i <= n; i++) {
            int x, y, r;
            std::cin >> x >> y >> r;
        }
    }
    return 0;
}